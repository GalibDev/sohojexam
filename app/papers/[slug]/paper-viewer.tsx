"use client";
import { useRef, useState } from "react";
import type { StaticPaper } from "../../../lib/paper-catalog";
export default function PaperViewer({paper}:{paper:StaticPaper}){const[zoom,setZoom]=useState(90),shell=useRef<HTMLElement>(null);const src=`${paper.asset}#toolbar=0&navpanes=0&scrollbar=1&zoom=${zoom}`;return <main className="own-pdf-viewer" ref={shell}><header><a href="/explore" aria-label="Back to question papers">←</a><div><h1>{paper.examType} - {paper.year}</h1><p>{paper.subject} · {paper.code}</p></div><a className="viewer-download" href={paper.asset} download>Download</a></header><nav><span className="page-label">▣ {paper.pages} {paper.pages===1?"page":"pages"}</span><div><button onClick={()=>setZoom(Math.max(50,zoom-10))} aria-label="Zoom out">−</button><b>{zoom}%</b><button onClick={()=>setZoom(Math.min(180,zoom+10))} aria-label="Zoom in">+</button></div><button onClick={()=>shell.current?.requestFullscreen()}>⛶ Fullscreen</button></nav><section><iframe key={zoom} title={`${paper.subject} question paper`} src={src}/></section><footer><span>Verified question paper · 2nd Year 1st Semester</span><b>{paper.subject}</b></footer></main>}
